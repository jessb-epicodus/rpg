import ClassName from '../src/js/file-name-calculator';

describe('ClassName', () => {

  let test;

  beforeEach(() => {
    test = new Test(TestArg);
  });

  test('should correctly blahblahblah', () => {
    expect(Test.getSomething).toEqual(ExpectedOutput);
  });

});